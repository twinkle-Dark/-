

# 互動式平面圖記錄系統 (cursor.html)

## 簡介
cursor.html 是一個基於瀏覽器的互動式平面圖記錄系統，用於追蹤和記錄參觀者在展覽空間中的行為與互動模式。系統支援多點位資料收集、時間追蹤、訪客資訊整合及數據提交功能。

## 主要功能

- **點位互動記錄**：記錄參觀者在各展位的互動行為
- **時間追蹤**：自動計算總參觀時間及各點位停留時間
- **互動行為分類**：提供多種行為標籤（觀看、聆聽、互動、紀錄等）
- **訪客資訊收集**：整合基本人口統計資料
- **本地數據暫存**：支援瀏覽器中斷後數據恢復
- **數據匯出**：支援將數據提交至 Google 表單

## 技術架構

- **純前端實現**：HTML + CSS + JavaScript
- **離線優先**：使用 localStorage 實現資料本地暫存
- **模組化設計**：功能按職責分離
- **響應式佈局**：支援桌面與行動裝置
- **數據通信**：與 Google Apps Script 整合

## 使用指南

### 基本操作流程

1. **開始記錄**：點擊"開始新記錄"按鈕
2. **點位記錄**：在平面圖上點擊相應點位開始計時
3. **填寫資訊**：選擇互動行為類型並填寫備註
4. **儲存記錄**：按下"儲存"按鈕保存此點位資料
5. **重複步驟2-4**：記錄更多點位
6. **結束記錄**：點擊"結束記錄"按鈕
7. **訪客授權**：向參觀者取得資料收集同意
8. **填寫訪客資訊**：完成基本資料收集表單
9. **提交資料**：系統自動提交所有記錄至後台

### 特殊功能

- **資料恢復**：系統啟動時檢測未儲存資料並提供恢復選項
- **錯誤重試**：自動保存失敗提交，下次啟動提供重試選項
- **記錄管理**：可刪除錯誤記錄

## 系統結構

```
cursor.html
├── CSS 樣式定義
│   ├── 平面圖元素樣式
│   ├── 點位樣式
│   ├── 表單樣式
│   ├── 提示訊息樣式
│   └── 響應式佈局
├── HTML 結構
│   ├── 平面圖容器
│   ├── 點位元素
│   ├── 記錄列表
│   ├── 活動記錄表單
│   ├── 訪客授權模態框
│   └── 訪客資料表單
└── JavaScript 功能
    ├── 初始化與狀態管理
    ├── 計時器功能
    ├── 記錄管理
    ├── 表單處理
    ├── UI 互動
    ├── 訪客資料處理
    └── 數據提交
```

## 關鍵函數說明

| 函數名 | 功能 |
|--------|------|
| `initialize()` | 系統初始化，檢查並載入本地存儲資料 |
| `startTimer()` | 開始全局計時器 |
| `openModal()` | 開啟點位記錄表單 |
| `saveRecord()` | 保存記錄至本地存儲 |
| `loadRecords()` | 讀取並顯示所有記錄 |
| `resetRecording()` | 重置系統狀態，開始新記錄 |
| `sendDataToGoogleSheet()` | 提交數據至 Google Sheet |
| `showToast()` | 顯示提示消息 |

## 數據結構

### 記錄物件結構
```javascript
{
  id: 數字ID,
  recordId: "A_B_YYYYMMDDhhmm_0000_000",
  museumId: "博物館代碼",
  userId: "記錄者ID",
  location: "點位ID",
  date: "YYYY-MM-DD",
  time: "HH:MM",
  arriveTime: "ISO時間格式",
  leaveTime: "ISO時間格式",
  staySeconds: 停留秒數,
  interactionCount: 互動次數,
  interactionType: "互動類型(逗號分隔)",
  notes: "備註文字",
  deleted: false
}
```

### 訪客資訊結構
```javascript
{
  visitorId: "訪客ID",
  recordId: "記錄ID",
  formVersion: "表單版本",
  totalSeconds: 總秒數,
  gender: "性別",
  ageGroup: "年齡層",
  education: "教育程度",
  residence: "居住地",
  visitPurpose: "參觀目的",
  visitFrequency: "參觀頻率"
}
```

## 擴展與維護指南

### 新增點位
在 `<div class="floorplan">` 內添加新點位元素，格式如下：
```html
<div class="hotspot" style="top: XXXpx; left: YYYpx;" data-location="區域-編號">
  <div class="hotspot-name">區域-編號</div>
</div>
```

### 修改互動類型
在活動記錄表單的 checkbox-group 區塊中添加或修改互動行為選項。

### 修改資料提交目標
更新 `sendDataToGoogleSheet()` 函數中的目標地址或整合方式。

## 故障排除

- **數據無法儲存**：檢查瀏覽器 localStorage 權限
- **計時器不準確**：檢查系統時間設定
- **提交失敗**：確認網路連接及 Google Apps Script 服務狀態
- **重複記錄**：檢查是否多次點擊儲存按鈕，考慮增加防抖動措施

---

© 2024 互動式平面圖記錄系統。保留所有權利。
