這份資料會依序實現以下功能，所以當讀取的功能有所缺失時，
請查看是否已有後續新增規劃，如有請依照規劃邏輯先行假設功能從在摒於附註標示，
如無則請給出新增建議，謝謝

• 開發環境
  - google apps script

  • 平面圖顯示
   - 顯示館廳的平面圖
   - 在平面圖上標示紅點作為停留點位
   - 用戶可以點擊紅點以輸入數據

  •即時碼表
    - 以橢圓形包覆顯示於右上角
    - 在輸入第一筆資料後開始計時
    - 自動填入總停留時間
    - 在每個點位輸入資料時同步計時
    - 按下結束紀錄時停止計時並將計時填入總停留時間
    
    * 計時邏輯：
        - 在用戶點擊第一個點位後自動開始計時
        - 計時格式為 HH:MM:SS
        - 在用戶點擊下一個點位或結束時，取得碼表
        - 馬錶會持續顯示累計時間讓使用者追蹤
        
    * 時間記錄：
        - 自動記錄到達時間 (arriveTime)
        - 自動記錄離開時間 (leaveTime)
        - 自動計算停留秒數 (staySeconds)
        - 自動計算總停留秒數 (totalSeconds)，為所有點位停留時間的累計
        - 計算停留秒數(leaveTime - arriveTime)
        - 計算總停留秒數(上筆記錄的總停留秒數+staySeconds的和)

      用戶點擊第一個點位
          │
          ▼
      檢查計時器是否已啟動
          │
          ├── 否 ──► 啟動計時器 (startTimer)
          │             │
          │             ▼
          │         記錄開始時間 (startTime)
          │             │
          │             ▼
          │         設置每秒更新 (setInterval)
          │
          ▼
      打開模態框 (openModal)
          │
          ▼
      記錄當前點位到達時間 (currentArriveTime)
          │
          ▼
      用戶填寫數據
          │
          ▼
      用戶點擊儲存
          │
          ▼
      記錄當前時間作為離開時間 (leaveTime)
          │
          ▼
      計算停留時間 (staySeconds = leaveTime - arriveTime)
          │
          ▼
      計算總停留時間 (totalSeconds)
          │
          ▼
      保存記錄
          │
          ▼
      關閉模態框



• 數據記錄
   - 用戶點擊地圖上的點位後，彈出輸入框填寫數據
   - 記錄的數據包括：
     | 欄位名稱          | 資料類型   | 說明            | 填寫方式        | 是否隱藏 |
     |-----------------|-----------|----------------|----------------|---------|
     | recordId        | TEXT      | 紀錄ID (主鍵)    | 系統自動生成     | 是 |
     | museumId        | TEXT      | 博物館ID        | 系統自動生成     | 是 |
     | userId          | TEXT      | 記錄者ID        | 系統自動生成     | 是 |
     | pointId         | TEXT      | 點位ID          | 系統自動代入    | 否 |
     | arriveTime      | DATETIME  | 到達時間         | 系統自動生成     | 是 |
     | leaveTime       | DATETIME  | 離開時間         | 系統自動生成     | 是 |
     | staySeconds     | NUMBER    | 停留秒數         | 系統自動計算     | 是 |
     | interactionCount| NUMBER    | 互動次數         | 使用者填寫      | 是 |
     | interactionType | TEXT      | 互動行為         | 使用者填寫(下拉選單)      | 否 |
     | notes           | TEXT      | 備註            | 使用者填寫      | 否 |

  運作方式
       1. 當用戶點擊平面圖上的點位時:
        - 系統自動生成recordId (格式:museumId_exhibitionHall_YYYYMMDDHHmm_userId_sequence)
          例如: nmp_nmp_000_202503311806_0000_001
          - nmp: 博物館代碼 
        - CH: 展廳代碼
        - 20240115: 日期 (台灣時區)
        - 1423: 時間 (台灣時區)
        - 201: 志工編號
        - 001: 每筆紀錄的順位編號第一筆為000依序
      - 系統自動填入museumId (從用戶選擇的館廳資訊取得)
      - 系統自動填入userId (從登入資訊取得)
      - 系統自動填入pointId (從點擊的點位取得)
      - 系統自動記錄arriveTime (碼表取得)
      - 系統自動記錄leaveTime (當用戶點擊下一個點位或結束時，取得碼表)
      - 系統自動計算staySeconds (點位碼表時間)**請在欄位中寫上，正在紀錄時間請待觀眾離開展位後再按下儲存

      2. 用戶需要填寫的欄位:
        - interactionType (勾選選單複選互動行為)
            - 互動行為包括：
            觀看
              - 觀看告示牌
              - 觀看展品
              - 觀看影片
              - 觀看模型
            聆聽
              - 聆聽導覽
              - 聆聽音樂
            互動 
              - 觸摸模型
              - 互動式投影
            紀錄
              - 拍照
              - 錄音
              - 錄影
              - 筆記
              - 其他
        - notes (可選填備註)

    3. 紀錄資料儲存流程:
      - 每筆記錄自動暫存於下方
      - 可即時查看已記錄的數據
      - 支援資料修改和刪除功能(按下刪除時將該筆資料標示為不回傳GOOGLESHEET而不是刪除)
      - 最後送出時封包寄送到GOOGLESHEET

• 結束紀錄流程:
        - 當用戶點擊右下方的結束紀錄按鈕時:
          - 系統會顯示灰色不透明遮罩，覆蓋整個畫面以防止其他操作
          - 彈出觀眾授權書視窗，提供同意或不同意資料蒐集的選項
          - 若用戶選擇不同意資料蒐集，點擊確認後系統將刪除所有緩存資料
          - 若用戶選擇同意資料蒐集，系統將顯示觀眾基本資料收集表單
          - 當觀眾點擊送出基本資料收集表單後，系統撤除灰色不透明遮罩

      • 填寫欄位:
        - 表單包含以下欄位，供用戶填寫或系統自動填入:
          | 欄位名稱       | 資料類型    | 說明                       | 填寫方式        | 是否隱藏 |
          |--------------|------------|---------------------------|----------------|---------|
          | visitorId    | TEXT      | 訪客ID (主鍵)               | 系統自動生成     | 是 |
          | recordId     | TEXT      | 關聯的紀錄ID                | 系統自動填入recordId| 是 |
          | formVersion  | TEXT      | 使用的表單版本               | 系統自動填入表單版本| 是 |
          | gender       | TEXT      | 性別                       | 使用者填寫(勾選) | 否 |
          | ageGroup     | TEXT      | 年齡層                     | 使用者填寫(下拉選單) | 否 |
          | education    | TEXT      | 教育程度                    | 使用者填寫(下拉選單) | 否 |
          | residence    | TEXT      | 居住地                     | 使用者填寫(下拉選單) | 否 |
          | visitPurpose | TEXT      | 參觀目的                    | 使用者填寫(勾選) | 否 |
          | visitFrequency | TEXT    | 參觀頻率                    | 使用者填寫(勾選) | 否 |
          | consent      | BOOLEAN   | 同意授權                    | 使用者填寫(勾選)   | 否 |
          | totalSeconds    | NUMBER    | 總停留秒數       | 系統自動計算     | 是 |
          | visitorComposition | TEXT     | 訪客組成       | 使用者填寫(勾選)     | 否 |

      • ID生成方式:
        - visitorId生成 (格式: 性別代碼_年齡代碼_教育代碼_居住地代碼_隨機數)
          例如: F_13_03_TT_1234
          - F: 性別代碼 (生理女性)
          - 13: 年齡層代碼 (13-18歲)
          - 03: 教育程度代碼 (高中/職)
          - TT: 居住地代碼 (台東縣)
          - 1234: 四位數隨機碼
          
        - recordId生成 (格式: 博物館代碼_展廳代碼_日期時間_志工編號_序列號)
          例如: NM_CH_202401151423_201_123
          - NM: 博物館代碼
          - CH: 展廳代碼
          - 202401151423: 日期時間 (YYYYMMDDHHMM，台灣時區)
          - 201: 志工編號
          - 123: 序列號，使用毫秒數確保唯一性
          
        - 系統自動填入formVersion(由表單資訊取得目前使用的表單版本)
        - 系統自動填入totalSeconds(按下結束紀錄時由碼表取得)

      // 用戶需要填寫的欄位:
      - gender (勾選選單)
        - 生理女性 (Female代號{F})
        - 生理男性 (Male代號{M})
        - 非二元性別 (Non-binary代號{N})
        - 其他 (Other代號{O})
        - 不願透漏 (Prefer not to disclose代號{d})

      - ageGroup (下拉選單)
        - 0-12 (代碼00)
        - 13-18 (代碼13)
        - 19-24 (代碼19)
        - 25-34 (代碼25)
        - 35-44 (代碼35)
        - 45-54 (代碼45)
        - 55-64 (代碼55)
        - 65-74 (代碼65)
        - 75-84 (代碼75)
        - 85-94 (代碼85)
        - 95以上 (代碼95)

      - education (下拉選單)
        - 國小或以下 (代碼01)
        - 國中 (代碼01)
        - 高中/職 (代碼02)
        - 專科 (代碼03)
        - 大學 (代碼04)
        - 研究所 (代碼05)
        - 其他 (代碼06)
        - 不願透漏 (代碼00)

      - residence (下拉選單) 字首放大 1.5 倍、字體顏色為多彩做為區隔
        - 台東縣 (代碼TT)
        - 花蓮縣 (代碼HL)
        - 宜蘭縣 (代碼IL)
        - 新北市 (代碼TP)
        - 基隆市 (代碼KL)
        - 台北市 (代碼TP)
        - 桃園市 (代碼TY)
        - 新竹縣 (代碼HJ)
        - 新竹市 (代碼HS)
        - 苗栗縣 (代碼ML)
        - 台中市 (代碼TC)
        - 彰化縣 (代碼CH)
        - 南投縣 (代碼NT)
        - 雲林縣 (代碼YL)
        - 嘉義縣 (代碼CY)
        - 嘉義市 (代碼CY)
        - 台南市 (代碼TN)
        - 高雄市 (代碼KS)
        - 屏東縣 (代碼PT)
        - 澎湖縣 (代碼PH)  
        - 金門縣 (代碼KH)
        - 連江縣 (代碼LY)
        - 其他 (代碼OT)
        - 不願透漏 (代碼di)

      - visitPurpose (勾選選單)
        - 學習
        - 休閒
        - 其他

      - visitFrequency (勾選選單)
        每年
        - 每年 1-2 次
        - 每年 3-4 次
        - 每年 5-6 次
        - 每年 7-8 次
        - 每年 9-10 次
        - 每年 11-12 次
        - 每年 13 以上
        - 不願透漏

      - consent (勾選選單)
        - 同意
        - 不同意

      - visitorComposition (勾選選單)
        - 單人
        - 家庭
        - 朋友
        - 同學
        - 同事
        - 觀光團
        - 其他

• 路線圖生成:
  - 系統依照數據記錄以每0.5秒/條的速率生成虛線，將順位000和001連接後+1
  - 重複生成直到所有紀錄點都連接完成，供觀眾互動
  - 將封包寄送到GOOGLESHEET，完成後刪除緩存資料 *

• 數據儲存
  - 生成編號 ID（例：AA202508301）並存入 Google 試算表
  - 自動產生每月報表
  ┌───────────────────┐
  │ [使用者輸入資料] │
  │       │           │
  │       ▼           │
  │ [HTML 前端]       │
  │ (input表單)       │
  │       │           │
  │       ▼           │
  │ [JavaScript 處理資料] │
  │ (打包成 JSON)     │
  │       │           │
  │       ▼           │
  │ [google.script.run] │
  │ 呼叫 Code.gs      │
  │       │           │
  │       ▼           │
  │ [doPost 或 自訂函數] │
  │ (Apps Script 接收資料) │
  │       │           │
  │       ▼           │
  │ [開啟試算表]      │
  │ (SpreadsheetApp.openById) │
  │       │           │
  │       ▼           │
  │ [寫入資料到試算表] │
  │ (appendRow)       │
  │       │           │
  │       ▼           │
  │ [回傳成功訊息給 HTML] │
  │       │           │
  │       ▼           │
  │ [顯示成功訊息給使用者] │
  └───────────────────┘

      JavaScript 收集資料 → 透過 document.getElementById().value 抓取使用者填寫的內容，然後整理成物件 (JSON 格式)。

      3️⃣ 透過 google.script.run 呼叫 Apps Script → 使用 google.script.run.函式名稱(資料)，將整理好的資料傳送到後端 (Code.gs)。

      4️⃣ Google Apps Script 接收資料 → 在 Code.gs 中撰寫函式來接收前端傳送的資料。

      5️⃣ 開啟試算表 → 使用 SpreadsheetApp.openById(試算表ID) 來開啟 Google 試算表，然後選擇正確的工作表 (Sheet)。

      6️⃣ 寫入資料 → 使用 appendRow() 將新資料寫入試算表的下一列。

      7️⃣ 回傳成功訊息 → Apps Script 回傳訊息給 HTML，通知資料已成功寫入。

      8️⃣ 前端顯示結果 → HTML 收到成功回應後，顯示「提交成功」的訊息給使用者。

      function saveData(data) {
        let sheet = SpreadsheetApp.openById("你的試算表ID").getSheetByName("工作表名稱");
        sheet.appendRow([data.name, data.email, data.message, new Date()]);
      }

• 開始新紀錄 按鈕
  - resetRecording 函數修改
      全面清理：
      清除所有 localStorage 項目
      清空記錄數組和記錄列表
      清空路線圖 SVG 元素
      重置所有界面元素：
      停止並重置計時器
      隱藏點位計時器
      清空記錄列表
      關閉所有模態窗口：
      確保活動記錄、授權書、訪客資料等模態窗口都被關閉
      提供用戶反饋：
      顯示成功重置的提示信息
      新增重置機制：
  - 通過"開始新記錄"按鈕主動重置
   - 通過"不同意"按鈕重置
   - 統一使用 resetRecording 函數處理重置邏輯

• 數據視覺化
   - 使用 Google Looker Studio 產生 BI 儀錶板
   - 展示統計圖表，支持自由調整篩選和顯示
   數據可視化：
在記錄列表中顯示每個點位的停留時間比例
使用圖表顯示觀眾在不同點位的時間分布

• 用戶登入
   - 使用 Google 帳戶進行驗證
   - 根據用戶角色分配相應的權限
       志工：可進行紀錄、
       管理員：可進行紀錄、查看後台、編輯點為和表單

• 館廳選擇
   - 提供下拉選單讓用戶選擇博物館和展廳(所有館廳)
   - 選定後識別用戶是否在帳號名單中
   - 若在名單中，則進入畫面，若不在名單中，則顯示"未授權"

測試策略：
   - 進行用戶測試，確保流程直觀
   - 測試不同設備和瀏覽器的兼容性
   - 進行負載測試，確保系統能處理多用戶同時使用   

   ----------------------------------------

   待辦
   - 點位分類
      例如多媒體說明、互動媒體、看板說明、藏品展示、影片播放等點位屬性(讓使用者自定義)
      點位顏色自定義 

右下角灰色小字顯示'檔案名稱'代表目前版本

偵測緩存檔案


在互動式平面圖標題後方添加一個不透明長寬和標題相同的隱藏按鈕，觸發條件為，滑鼠(或手指)在上面常按3秒後便代表按下，按下後顯示後台管理介面
• 開發後台
  - 添加密碼保護
  - 數據、函示監控
  - 編輯表單
  - 編輯平面圖點位
  - 功能開關(路線圖生成模組化並關閉)
  - 加速數據發送
  - 發送過程中禁用開始新紀錄按鈕防止數據被清空導致失敗
  - 統一錯誤處理
    . 錯誤處理統一
      目前代碼中有多處相似的錯誤處理邏輯，包括：
      錯誤日誌記錄 (console.error)
      用戶提示 (showToast)
      UI元素恢復 (按鈕狀態重置等)
      建議創建一個統一的錯誤處理函數：
          /**
          * 統一處理錯誤的函數
          * @param {Error} error - 錯誤對象
          * @param {string} context - 錯誤發生的上下文/位置
          * @param {Object} options - 額外選項
          * @param {HTMLElement} options.element - 需要恢復狀態的元素（如按鈕）
          * @param {string} options.originalText - 元素的原始文字
          * @param {number} options.toastDuration - Toast 顯示時間
          * @param {Function} options.callback - 錯誤處理後的回調函數
          */
          function handleError(error, context, options = {}) {
            // 1. 記錄錯誤
            console.error(`${context}時發生錯誤:`, error);
            
            // 2. 顯示用戶提示
            const message = `${context}時發生錯誤: ${error.message}`;
            const duration = options.toastDuration || 3000;
            showToast(message, duration);
            
            // 3. 恢復 UI 元素狀態
            if (options.element) {
              options.element.textContent = options.originalText || '儲存';
              options.element.disabled = false;
            }
            
            // 4. 執行額外回調
            if (options.callback && typeof options.callback === 'function') {
              options.callback(error);
            }
          }

• bug 
  無WIFI環境中重新提交數據流程
  跨天資料自動刪除

